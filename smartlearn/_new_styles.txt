<style>
    @import url('https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@300;400;500;600;700;800&family=Fraunces:opsz,wght@9..144,400;9..144,500;9..144,600;9..144,700;9..144,800&display=swap');

    /* ============================================================
       SMARTLEARN — REDESIGN BASE (Modern Editorial)
       Inspired by Coursera × Notion × Linear
       ============================================================ */
    body {
        font-family: 'Plus Jakarta Sans', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
        background: #FAFAF7;
        color: #18181B;
        -webkit-font-smoothing: antialiased;
        -moz-osx-font-smoothing: grayscale;
    }

    /* ---- HERO ---- */
    .sl-hero {
        max-width: 1200px;
        margin: 0 auto;
        padding: 3rem 1.5rem 2.5rem;
        display: grid;
        grid-template-columns: 1.4fr 1fr;
        gap: 4rem;
        align-items: center;
    }
    @media (max-width: 900px) {
        .sl-hero { grid-template-columns: 1fr; gap: 2.5rem; padding: 2rem 1.25rem 1rem; text-align: left; }
    }

    .sl-eyebrow {
        display: inline-flex; align-items: center; gap: .55rem;
        padding: .45rem 1rem;
        background: #FFFFFF;
        border: 1px solid #EAEAE0;
        border-radius: 999px;
        font-size: .78rem; font-weight: 600;
        color: #404049;
        text-transform: uppercase;
        letter-spacing: .08em;
        margin-bottom: 1.5rem;
        box-shadow: 0 1px 2px rgba(0,0,0,.03);
    }
    .sl-eyebrow .sl-pulse {
        width: 8px; height: 8px;
        background: #10B981; border-radius: 999px;
        box-shadow: 0 0 0 4px rgba(16,185,129,.18);
        animation: slPulse 2s ease-in-out infinite;
    }
    @keyframes slPulse {
        0%,100% { box-shadow: 0 0 0 4px rgba(16,185,129,.18); }
        50% { box-shadow: 0 0 0 7px rgba(16,185,129,.08); }
    }

    .sl-hero-h1 {
        font-family: 'Fraunces', Georgia, serif;
        font-size: clamp(2.4rem, 5.2vw, 4.4rem);
        font-weight: 600;
        line-height: 1.02;
        color: #0A0A0F;
        margin: 0 0 1.25rem;
        letter-spacing: -0.025em;
    }
    .sl-hero-h1 em {
        font-style: italic;
        font-weight: 500;
        color: #2563EB;
    }

    .sl-hero-sub {
        font-size: 1.1rem;
        line-height: 1.6;
        color: #525258;
        max-width: 540px;
        margin-bottom: 2rem;
    }

    .sl-hero-stats {
        display: flex; gap: 2.5rem; flex-wrap: wrap;
        padding-top: 1.5rem;
        border-top: 1px solid #ECECE5;
    }
    .sl-hero-stat .sl-num {
        font-family: 'Fraunces', serif;
        font-size: 1.8rem; font-weight: 600;
        color: #0A0A0F; line-height: 1;
        letter-spacing: -0.02em;
    }
    .sl-hero-stat .sl-lab {
        font-size: .78rem; color: #71717A;
        font-weight: 500; margin-top: .35rem;
        text-transform: uppercase; letter-spacing: .06em;
    }

    /* Hero visual side */
    .sl-hero-visual {
        position: relative;
        aspect-ratio: 1/1;
        max-width: 420px;
        margin-left: auto;
    }
    @media (max-width: 900px) { .sl-hero-visual { display: none; } }

    .sl-hv-card {
        position: absolute;
        background: #FFF;
        border: 1px solid #ECECE5;
        border-radius: 22px;
        box-shadow: 0 30px 60px -25px rgba(20,20,30,.18);
        padding: 1.25rem;
    }
    .sl-hv-card.c1 {
        top: 5%; left: 5%; width: 65%; height: 75%;
        transform: rotate(-6deg);
        background: linear-gradient(160deg, #FFF 0%, #F8FAFF 100%);
    }
    .sl-hv-card.c2 {
        bottom: 8%; right: 5%; width: 55%;
        transform: rotate(4deg);
        padding: 1rem 1.25rem;
    }
    .sl-hv-page-lines { display:flex; flex-direction:column; gap:.6rem; margin-top:.75rem; }
    .sl-hv-page-lines span {
        display:block; height: 8px; background: #E8E8E0; border-radius: 4px;
    }
    .sl-hv-page-lines span:nth-child(1){ width: 90%; }
    .sl-hv-page-lines span:nth-child(2){ width: 75%; }
    .sl-hv-page-lines span:nth-child(3){ width: 85%; background: linear-gradient(90deg,#2563EB,#06B6D4); }
    .sl-hv-page-lines span:nth-child(4){ width: 60%; }
    .sl-hv-page-lines span:nth-child(5){ width: 80%; }
    .sl-hv-page-lines span:nth-child(6){ width: 45%; }
    .sl-hv-tag {
        display: inline-flex; align-items: center; gap: .4rem;
        background: #0F172A; color: #FFF;
        padding: .4rem .75rem; border-radius: 999px;
        font-size: .72rem; font-weight: 600;
        text-transform: uppercase; letter-spacing: .06em;
    }
    .sl-hv-tag i { color: #FACC15; }
    .sl-hv-mini-stat { display:flex; align-items:center; gap:.75rem; margin-top:.85rem; }
    .sl-hv-mini-stat .sl-hv-ic {
        width: 36px; height: 36px; border-radius: 10px;
        background: #DBEAFE; color: #2563EB;
        display:flex; align-items:center; justify-content:center;
        font-size: .9rem;
    }
    .sl-hv-mini-stat strong { font-family: 'Fraunces',serif; font-weight: 600; font-size: 1.1rem; color: #0A0A0F; display:block; line-height:1;}
    .sl-hv-mini-stat small { color: #71717A; font-size: .72rem; font-weight: 500; }

    .sl-hv-blob {
        position: absolute;
        border-radius: 50%;
        filter: blur(60px); opacity: .35;
        pointer-events: none;
    }
    .sl-hv-blob.b1 { width: 280px; height: 280px; background: #BFDBFE; top: -40px; right: -40px; }
    .sl-hv-blob.b2 { width: 220px; height: 220px; background: #FEF3C7; bottom: -30px; left: -30px; }

    /* ---- UPLOAD CARD ---- */
    .sl-section {
        max-width: 880px;
        margin: 0 auto;
        padding: 0 1.5rem;
    }

    .sl-upload-shell {
        background: #FFFFFF;
        border: 1px solid #ECECE5;
        border-radius: 28px;
        padding: 2rem;
        box-shadow: 0 1px 0 #F3F3EE inset, 0 30px 60px -25px rgba(20,20,30,.10);
        position: relative;
        overflow: hidden;
    }
    .sl-upload-shell::before {
        content: '';
        position: absolute; inset: 0 0 auto 0;
        height: 4px;
        background: linear-gradient(90deg, #2563EB 0%, #06B6D4 45%, #F59E0B 100%);
    }

    .sl-drop {
        border: 2px dashed #D8D8CE;
        background: #FAFAF6;
        border-radius: 20px;
        padding: 2.75rem 1.75rem;
        text-align: center;
        transition: all .25s ease;
    }
    .sl-drop:hover { border-color: #2563EB; background: #F4F7FE; }

    .sl-drop-icon {
        width: 76px; height: 76px;
        margin: 0 auto 1.5rem;
        background: #0F172A;
        border-radius: 22px;
        display:flex; align-items:center; justify-content:center;
        color: #FFF; font-size: 1.85rem;
        box-shadow: 0 18px 40px -10px rgba(15,23,42,.45);
        transform: rotate(-5deg);
        position: relative;
    }
    .sl-drop-icon::after {
        content: ''; position: absolute; right: -8px; top: -8px;
        width: 22px; height: 22px;
        background: #FACC15; border-radius: 50%;
        border: 3px solid #FAFAF6;
    }

    .sl-drop h3 {
        font-family: 'Fraunces', serif;
        font-size: 1.85rem;
        font-weight: 600;
        color: #0A0A0F;
        margin: 0 0 .5rem;
        letter-spacing: -0.02em;
    }
    .sl-drop p {
        color: #6B7280;
        font-size: 1rem;
        margin: 0 auto 1.75rem;
        max-width: 460px;
        line-height: 1.55;
    }

    .sl-btn {
        display: inline-flex; align-items: center; justify-content: center;
        gap: .55rem;
        padding: .95rem 1.7rem;
        border-radius: 14px;
        font-weight: 600;
        font-size: 1rem;
        font-family: inherit;
        cursor: pointer;
        transition: transform .15s ease, background .2s, box-shadow .2s, border-color .2s;
        border: 0;
        text-decoration: none;
        line-height: 1;
    }
    .sl-btn-primary {
        background: #0F172A; color: #FFFFFF;
        box-shadow: 0 10px 22px -10px rgba(15,23,42,.55);
    }
    .sl-btn-primary:hover { transform: translateY(-1px); background: #1E293B; }
    .sl-btn-outline {
        background: #FFFFFF; color: #0F172A;
        border: 1.5px solid #E5E5DA;
    }
    .sl-btn-outline:hover { border-color: #0F172A; transform: translateY(-1px); }
    .sl-btn-block { width: 100%; }
    .sl-btn-lg { padding: 1.15rem 1.85rem; font-size: 1.05rem; border-radius: 16px; }

    .sl-cta-row {
        display: flex; gap: .75rem;
        justify-content: center;
        flex-wrap: wrap;
    }

    .sl-trust {
        display: flex; gap: .55rem; flex-wrap: wrap;
        margin-top: 1.75rem;
        justify-content: center;
    }
    .sl-chip {
        display: inline-flex; align-items: center; gap: .45rem;
        padding: .4rem .85rem;
        background: #F4F4EE;
        color: #525258;
        border-radius: 999px;
        font-size: .8rem; font-weight: 500;
        border: 1px solid #ECECE5;
    }
    .sl-chip i { color: #2563EB; font-size: .68rem; }
    .sl-chip.sl-chip-success i { color: #10B981; }
    .sl-chip.sl-chip-warn i { color: #F59E0B; }

    .sl-divider-or {
        display:flex; align-items:center; gap: 1rem;
        margin: 1.5rem 0 0;
        color: #A1A1AA; font-size: .75rem; font-weight: 600;
        text-transform: uppercase; letter-spacing: .08em;
    }
    .sl-divider-or::before, .sl-divider-or::after {
        content:''; flex: 1; height: 1px; background: #ECECE5;
    }

    .sl-text-link {
        background: none; border: 0; color: #2563EB;
        font-family: inherit; font-weight: 600; font-size: .92rem;
        cursor: pointer; padding: .25rem .5rem;
        text-decoration: underline; text-underline-offset: 3px;
    }
    .sl-text-link:hover { color: #1D4ED8; }

    /* ---- ROLE / CONFIG ---- */
    .sl-config-card {
        background: #FFFFFF;
        border: 1px solid #ECECE5;
        border-radius: 28px;
        padding: 2rem;
        box-shadow: 0 30px 60px -25px rgba(20,20,30,.08);
    }
    .sl-config-head {
        display: flex; flex-wrap: wrap; gap: 1.25rem;
        justify-content: space-between; align-items: flex-start;
        margin-bottom: 2rem;
    }
    .sl-config-head h2 {
        font-family: 'Fraunces', serif;
        font-size: 2rem; font-weight: 600;
        color: #0A0A0F; letter-spacing: -0.02em;
        margin: 0 0 .35rem;
    }
    .sl-config-head p { color: #71717A; font-size: .98rem; margin: 0; }

    .sl-role-seg {
        display: inline-flex;
        background: #F4F4EE;
        padding: 4px;
        border-radius: 14px;
        border: 1px solid #ECECE5;
    }
    .sl-role-btn {
        padding: .65rem 1.4rem;
        border-radius: 10px;
        font-weight: 600;
        font-size: .92rem;
        font-family: inherit;
        border: 0; background: transparent;
        color: #6B7280; cursor: pointer;
        transition: all .2s; display:flex; align-items:center; gap:.45rem;
    }
    .sl-role-btn i { font-size: .85rem; }
    .sl-role-btn.active {
        background: #FFF; color: #0A0A0F;
        box-shadow: 0 1px 3px rgba(0,0,0,.08), 0 0 0 1px #ECECE5;
    }

    .sl-section-label {
        display: flex; align-items: center; justify-content: space-between;
        margin-bottom: 1rem;
    }
    .sl-section-label h3 {
        font-family: 'Fraunces', serif;
        font-size: 1.25rem; font-weight: 600;
        color: #0A0A0F; margin: 0;
        letter-spacing: -0.01em;
    }
    .sl-counter-pill {
        display:inline-flex; align-items:center; gap: .4rem;
        background: #F4F4EE; color: #404049;
        padding: .35rem .85rem;
        border-radius: 999px;
        font-size: .8rem; font-weight: 600;
        border: 1px solid #ECECE5;
    }
    .sl-counter-pill strong { color: #2563EB; font-weight: 700; }

    .sl-teacher-textarea {
        width: 100%;
        padding: 1.25rem;
        background: #FAFAF6;
        border: 1.5px solid #E5E5DA;
        border-radius: 18px;
        outline: none;
        transition: all .2s;
        font-family: inherit;
        font-size: 1rem;
        line-height: 1.6;
        color: #18181B;
        min-height: 180px;
        resize: vertical;
    }
    .sl-teacher-textarea:focus {
        border-color: #2563EB;
        background: #FFFFFF;
        box-shadow: 0 0 0 4px rgba(37,99,235,.1);
    }

    /* ---- HISTORY ---- */
    .sl-history-head {
        max-width: 1200px;
        margin: 3rem auto 1.25rem;
        padding: 0 1.5rem;
        display: flex; align-items: baseline; justify-content: space-between; flex-wrap: wrap; gap: .5rem;
    }
    .sl-history-head h2 {
        font-family: 'Fraunces', serif;
        font-size: 2rem; font-weight: 600;
        color: #0A0A0F; margin: 0;
        letter-spacing: -0.02em;
    }
    .sl-history-head .sl-history-sub { color: #71717A; font-size: .95rem; }

    .sl-history-wrap {
        max-width: 1200px;
        margin: 0 auto 3rem;
        padding: 0 1.5rem;
    }
    .sl-history-grid {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(290px,1fr));
        gap: 1.25rem;
    }
    .sl-history-card {
        background: #FFFFFF;
        border: 1px solid #ECECE5;
        border-radius: 22px;
        padding: 1.5rem 1.5rem 1.25rem;
        display: flex; flex-direction: column;
        transition: all .25s ease;
        position: relative; overflow: hidden;
    }
    .sl-history-card::before {
        content:''; position: absolute; inset: 0 0 auto 0; height: 4px;
        background: linear-gradient(90deg, #2563EB, #06B6D4);
        opacity: .95;
    }
    .sl-history-card:nth-child(3n+2)::before { background: linear-gradient(90deg, #F59E0B, #EF4444); }
    .sl-history-card:nth-child(3n+3)::before { background: linear-gradient(90deg, #10B981, #2563EB); }
    .sl-history-card:hover {
        transform: translateY(-4px);
        box-shadow: 0 25px 50px -15px rgba(20,20,30,.14);
        border-color: #D8D8CE;
    }
    .sl-history-card h3 {
        font-family: 'Fraunces', serif;
        font-size: 1.2rem;
        font-weight: 600;
        color: #0A0A0F;
        line-height: 1.3;
        margin: .85rem 0 1.5rem;
        letter-spacing: -0.01em;
        display: -webkit-box;
        -webkit-line-clamp: 3;
        -webkit-box-orient: vertical;
        overflow: hidden;
    }
    .sl-h-meta {
        display:inline-flex; align-items:center; gap: .4rem;
        font-size: .7rem; font-weight: 600;
        color: #71717A;
        text-transform: uppercase; letter-spacing: .06em;
    }
    .sl-h-meta i { color: #2563EB; }
    .sl-h-btn {
        width: 100%; padding: .8rem 1rem;
        background: #0F172A; color: #FFFFFF;
        border: 0; border-radius: 12px;
        font-weight: 600; font-size: .92rem;
        font-family: inherit;
        cursor: pointer; transition: all .2s;
        display: flex; align-items: center; justify-content: center; gap: .5rem;
    }
    .sl-h-btn:hover { background: #1E293B; transform: translateY(-1px); }
    .sl-h-mini {
        display: flex; gap: .5rem; margin-top: .55rem;
    }
    .sl-h-mini button {
        flex:1; padding: .55rem;
        background: #F4F4EE; color: #525258;
        border: 0; border-radius: 10px;
        font-weight: 600; font-size: .78rem;
        font-family: inherit;
        cursor: pointer; transition: all .2s;
        display: flex; align-items: center; justify-content: center; gap: .4rem;
    }
    .sl-h-mini button:hover { background: #ECECE5; color: #0A0A0F; }
    .sl-h-mini button.sl-danger:hover { background: #FEF2F2; color: #DC2626; }

    /* ---- Loader ---- */
    #loader.sl-loader {
        margin-top: 1.5rem; padding: 2rem;
        background: #F4F7FE;
        border: 1px solid #DBEAFE;
        border-radius: 20px;
        text-align: center;
    }
    .sl-spinner {
        position: relative; width: 64px; height: 64px;
        margin: 0 auto 1rem;
    }
    .sl-spinner::before, .sl-spinner::after {
        content:''; position: absolute; inset: 0;
        border-radius: 50%;
    }
    .sl-spinner::before { border: 4px solid #DBEAFE; }
    .sl-spinner::after {
        border: 4px solid transparent; border-top-color: #2563EB;
        animation: slSpin .9s linear infinite;
    }
    @keyframes slSpin { to { transform: rotate(360deg);} }

    /* ---- Preview Grid ---- */
    .sl-preview-grid {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(140px,1fr));
        gap: .75rem;
        margin-bottom: 1.5rem;
    }
    .sl-preview-grid img,
    .sl-preview-grid > div {
        border-radius: 14px;
        border: 1px solid #ECECE5;
    }

    /* ============================================================
       PRESERVED LEGACY STYLES (for exercise sections, modals, etc.)
       ============================================================ */
    .hero-text-small { font-size: 2.2rem; font-weight: 700; color: #111827; line-height: 1.1; }
    .hero-text-xl { font-size: 6rem; font-weight: 900; color: #111827; line-height: 1; letter-spacing: -0.05em; }
    .hero-text-md { font-size: 3rem; font-weight: 700; color: #111827; line-height: 1.1; }
    .hero-text-sm-bold { font-size: 1.8rem; font-weight: 700; color: #111827; line-height: 1.1; }
    .book-icon-box { background: white; border-radius: 2.5rem; width: 180px; height: 180px; display: flex; align-items: center; justify-content: center; box-shadow: 0 20px 40px rgba(0, 0, 0, 0.04); border: 1px solid rgba(226, 232, 240, 0.8); }
    .upload-container { border: 1px solid #e2e8f0; border-radius: 2rem; padding: 3rem; background: white; box-shadow: 0 10px 30px rgba(0, 0, 0, 0.02); }
    .plus-btn-circle { width: 60px; height: 60px; background: #4f46e5; border-radius: 50%; display: flex; align-items: center; justify-content: center; margin: 0 auto 1.5rem; box-shadow: 0 8px 20px rgba(79, 70, 229, 0.3); transition: transform 0.2s; }

    .glass-card {
        background: rgba(255, 255, 255, 0.95);
        backdrop-filter: blur(12px);
        border: 1px solid rgba(226, 232, 240, 0.8);
        border-radius: 2rem;
        box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.05), 0 10px 15px -3px rgba(0, 0, 0, 0.1);
    }
    .exercise-card:hover {
        transform: translateY(-5px);
        box-shadow: 0 25px 50px rgba(0, 0, 0, 0.08);
    }
    .btn-primary {
        background: linear-gradient(135deg, #4f46e5 0%, #3b82f6 100%);
        transition: all 0.3s ease;
    }
    .btn-primary:hover {
        opacity: 0.9;
        transform: scale(1.02);
        box-shadow: 0 10px 20px rgba(59, 130, 246, 0.3);
    }
